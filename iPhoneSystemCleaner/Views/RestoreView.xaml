<UserControl x:Class="iPhoneSystemCleaner.Views.RestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Khôi phục dữ liệu đã xóa (Restore)"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Quản lý và khôi phục các bản sao lưu .tar.gz trên máy tính"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="13" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="BackupList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,12"
                                Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="📦" FontSize="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='Ngày tạo: {0:dd/MM/yyyy HH:mm}'}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text=" • " Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding SizeDisplay}" Foreground="{StaticResource AccentBlueBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Khôi phục" 
                                            Tag="{Binding FullPath}"
                                            Click="BtnRestore_Click"
                                            Padding="16,8"
                                            Foreground="{StaticResource TextPrimaryBrush}"
                                            Background="#2D2D30"
                                            BorderThickness="0"
                                            Margin="0,0,8,0"
                                            Cursor="Hand"/>
                                    <Button Content="Xóa file" 
                                            Tag="{Binding FullPath}"
                                            Click="BtnDeleteFile_Click"
                                            Padding="16,8"
                                            Foreground="White"
                                            Background="#E53935"
                                            BorderThickness="0"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state -->
        <TextBlock x:Name="TbEmpty" Grid.Row="1" 
                   Text="Chưa có bản sao lưu nào." 
                   Foreground="{StaticResource TextSecondaryBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <!-- Log Panel -->
        <Border Grid.Row="2" x:Name="LogPanel"
                Background="#111111"
                BorderBrush="#252526"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,16,0,0"
                Padding="12,8"
                Visibility="Collapsed"
                MaxHeight="100">
            <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="TbLog"
                           FontFamily="Consolas"
                           FontSize="11"
                           Foreground="#00E5A0"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

    </Grid>
</UserControl>
